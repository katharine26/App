package com.example.drawingapp

import android.graphics.Bitmap;
//App no longer uses this JNI
/*public class DrawingJNI {
    companion object {
        // Load the c++ library which has the functions we created
        init {
            System.loadLibrary("drawingapp");
        }

        // These are the functions we can call in our app which are connected with the c++
        external fun clearCanvas(bitmap: Bitmap)
        external fun fillCanvas(bitmap: Bitmap, color: Int)

    }

}*/