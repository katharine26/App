<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DrawingApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">#000080</item>
        <item name="colorOnPrimary">#FFFFFF</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.DrawingApp" parent="Base.Theme.DrawingApp" />

    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/orange</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/orange</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.DrawingApp</item>
    </style>
</resources>